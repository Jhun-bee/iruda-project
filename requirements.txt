# 이루다(Iruda) 자립청소년 AI 컨설팅 시스템
# 필수 의존성 라이브러리

# Flask 웹 프레임워크 (필수)
Flask==2.3.3
Flask-Login==0.6.2
Werkzeug==2.3.7

# AI/ML 라이브러리 (핵심 기능)
torch==2.8.0                    # PyTorch - sentence-transformers 의존성
torchaudio==2.8.0
torchvision==0.23.0
sentence-transformers==5.1.1    # 의미적 검색 엔진
scikit-learn==1.3.0            # 코사인 유사도 계산
numpy==1.24.3                  # 수치 연산

# OpenAI API (대화형 AI)
openai==1.3.0                  # 최신 OpenAI API v1

# 데이터 처리
pandas==2.1.1                 # Excel/CSV 데이터 처리
openpyxl==3.1.2               # Excel 파일 읽기

# HTTP 및 환경 설정
requests==2.31.0              # HTTP 요청
python-dotenv==1.0.0          # 환경 변수 관리

# 날짜/시간 처리
python-dateutil==2.8.2        # 고급 날짜 처리

# 개발 및 디버깅 (선택사항)
tqdm==4.66.1                  # 진행률 표시바
flask-debugtoolbar==0.13.1    # Flask 디버깅 도구

# 프로덕션 배포용 (선택사항)
# 실제 서버 배포시에만 설치
# gunicorn==21.2.0            # WSGI 프로덕션 서버
# psycopg2-binary==2.9.7      # PostgreSQL 드라이버 (SQLite 대신)
# redis==4.6.0                # 캐싱 시스템

# 코드 품질 관리 (개발환경용)
pytest==7.4.2              # 테스트 프레임워크
black==23.7.0               # 코드 포매터
flake8==6.0.0               # 코드 린터

# 주의사항:
# 1. torch 설치시 시간이 오래 걸릴 수 있습니다 (500MB+)
# 2. sentence-transformers 첫 실행시 모델 다운로드 필요 (1.4GB)
# 3. GPU가 있다면 torch-gpu 버전 사용 권장 (선택사항)

# 설치 명령어:
# pip install -r requirements.txt

# 최소 시스템 요구사항:
# - Python 3.8+
# - RAM 4GB+ (AI 모델용)
# - 저장공간 3GB+ (모델 캐시용)